# ===========================================
# LOCAL DEVELOPMENT ENVIRONMENT
# ===========================================
# Copy this file to .env for local development
# For production (Coolify), see .env.example

# ===========================================
# DATABASE (Local PostgreSQL Container)
# ===========================================
# These values configure the local postgres container
POSTGRES_DB=postgres
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password
POSTGRES_PORT=5433

# Full database URL (constructed automatically if not set)
# DATABASE_URL=postgresql://postgres:password@postgres:5432/postgres

# ===========================================
# REDIS (Local Redis Container)
# ===========================================
REDIS_PORT=6380
# REDIS_URL=redis://redis:6379

# ===========================================
# SERVER
# ===========================================
SERVER_PORT=4000
NODE_ENV=development

# ===========================================
# JWT AUTHENTICATION
# ===========================================
# For development only - use a secure secret in production!
JWT_SECRET=dev-secret-change-in-production
JWT_EXPIRES_IN=15m

# ===========================================
# APPLICATION URLS
# ===========================================
CLIENT_URL=http://localhost:3000
NEXT_PUBLIC_API_URL=http://localhost:4000/api
NEXT_PUBLIC_SOCKET_URL=http://localhost:4000

# ===========================================
# LIVEKIT (Optional for local development)
# ===========================================
# Leave empty for local dev or add your LiveKit credentials
LIVEKIT_API_KEY=
LIVEKIT_API_SECRET=
LIVEKIT_URL=
NEXT_PUBLIC_LIVEKIT_URL=

# ===========================================
# RESEND (Optional for local development)
# ===========================================
RESEND_API_KEY=

# ===========================================
# QUICK START
# ===========================================
# 1. Copy this file: cp .env.local.example .env
# 2. Update values if needed (defaults work out of the box)
# 3. Run: docker-compose up -d
# 4. Access app at http://localhost:3000
